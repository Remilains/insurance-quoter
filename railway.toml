[build]
builder = "NIXPACKS"

[deploy]
startCommand = "playwright install chromium && playwright install-deps chromium && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 4"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
